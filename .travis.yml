node_js:
  - "6.11.0"

install:
  - npm install -g https://github.com/incf/bids-validator.git

script:
  - |
    rc=0;
    for i in $(ls -d */);
    do
      echo "Validating dataset" $i
      bids-validator ${i%%/} --bep006 --ignoreNiftiHeaders || rc=$?
    done
    exit $rc;
