Example SWI dataset

This dataset demonstrates the organization of susceptibility weighted imaging
(SWI) data in BIDS. It includes multi-echo gradient-echo (MEGRE) raw data with
magnitude and phase components, derived SWI, and quantitative susceptibility map (Chimap) outputs.
This example covers cases where the SWI images are generated directly from scanner.
